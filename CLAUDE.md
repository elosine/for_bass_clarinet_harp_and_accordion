# for bass clarinet, harp, and accordion

Composition #3 in the custom-composition-system lineage
(#1 `string_quartet_no1-composer` → #2 `composition_for_two_pianos_and_two_percussion` → this).

## Orient from docs, not from scanning

- **Living plan:** `docs/PLAN.md` — stable IDs; rules in its header
- **Session state, decisions, principles:** `docs/PROJECT_JOURNAL.md` — §2 Resume Here first
- **Working preferences:** `docs/HOW_WE_WORK.md`
- **Session routines:** `docs/SESSION_PROTOCOL.md` (`/session-start`, `/session-end`)

Do NOT scan or analyze the codebase unprompted. Name the question first, then read only
what answers it. High bar for subagents / background processes (see HOW_WE_WORK).

## Reference repos (read-only context; registered as additional working dirs)

- Piece #1: `C:\Users\jwloy\GitHub\string_quartet_no1-composer`
- Piece #2: `C:\Users\jwloy\GitHub\composition_for_two_pianos_and_two_percussion` (most recent; richest `docs/`)

Consult only when a specific named question requires it. Never edit them.

## Git

- Commit at the natural wrap of an approved chunk; reference plan IDs (e.g. `1c`) in messages.
- **Never push without asking.** Session End always asks "push now?".
